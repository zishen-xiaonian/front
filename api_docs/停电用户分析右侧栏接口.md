# 停电用户分析右侧栏接口

## 接口信息

- 请求方式：`POST`
- 默认地址：`/api/v1/outage-users/analysis-overview`
- 前端环境变量：`VITE_OUTAGE_USER_ANALYSIS_API`
- 超时时间：60 秒

右侧“频繁停电预警”图表单独使用：

- 请求方式：`POST`
- 默认地址：`/api/v1/outage-analysis/frequent-outage-warning-daily-trend`
- 前端环境变量：`VITE_OUTAGE_ANALYSIS_WARNING_DAILY_TREND_API`
- 数据来源：内网业务库 `outage_user_daily_warning`

七日/三十日请求：

```json
{
  "rangeType": "sevenDays",
  "cityId": "唐山市ID",
  "endDate": "2026-09-15"
}
```

历史请求：

```json
{
  "rangeType": "history",
  "cityId": "唐山市ID",
  "beginDate": "2026-09-09",
  "endDate": "2026-09-15"
}
```

`beginDate` 和 `endDate` 只允许 `YYYY-MM-DD`；右侧历史范围必须位于最近一个月内，至少包含连续 7 个自然日，也可以选择 8 天、9 天或更长区间。

开发阶段默认使用示例数据：

```env
VITE_OUTAGE_USER_ANALYSIS_USE_MOCK=true
```

后端接口完成后改为 `false` 并重新启动 Vite：

```env
VITE_OUTAGE_USER_ANALYSIS_USE_MOCK=false
```

## 请求参数

```json
{
  "rangeType": "thirtyDays",
  "countyName": "",
  "beginTime": "2026-08-13 00:00:00",
  "endTime": "2026-09-11 23:59:59"
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `rangeType` | string | `sevenDays`、`thirtyDays` 或 `history` |
| `countyName` | string | 区县名称；空字符串表示全部区县 |
| `beginTime` | string | 开始时间；历史范围时传空字符串 |
| `endTime` | string | 结束时间 |

## 推荐响应结构

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userTypes": {
      "labels": ["14", "16", "18", "20"],
      "series": [
        {
          "key": "coal",
          "name": "煤改电用户",
          "color": "#1eb1bf",
          "values": [92, 118, 146, 154]
        },
        {
          "key": "important",
          "name": "重要用户",
          "color": "#e6aa17",
          "values": [28, 34, 48, 66]
        }
      ]
    },
    "frequentWarnings": {
      "labels": ["14", "16", "18", "20"],
      "series": [
        { "key": "extreme", "name": "极度风险", "values": [11, 17, 24, 28] },
        { "key": "exceptional", "name": "特大风险", "values": [18, 24, 31, 37] },
        { "key": "severe", "name": "重大风险", "values": [22, 29, 38, 45] },
        { "key": "major", "name": "较大风险", "values": [27, 36, 44, 52] },
        { "key": "medium", "name": "中度风险", "values": [34, 45, 54, 64] },
        { "key": "mild", "name": "轻度风险", "values": [40, 51, 62, 73] }
      ]
    },
    "outageImpact": {
      "labels": ["14", "16", "18", "20"],
      "series": [
        { "key": "veryHigh", "name": "用户数极大", "values": [18, 24, 31, 37] },
        { "key": "high", "name": "用户数较大", "values": [31, 42, 55, 63] },
        { "key": "medium", "name": "用户数中度", "values": [54, 68, 86, 94] },
        { "key": "low", "name": "用户数轻度", "values": [38, 47, 58, 66] }
      ]
    }
  }
}
```

说明：

- 三个图表允许使用不同的 `labels`。
- 每个 `series.values` 应与对应 `labels` 等长。
- `color` 可选；不返回时前端使用与设计稿一致的默认颜色。
- 接口失败或响应数据缺失时，前端会自动回退到示例数据并显示“接口未就绪 · 示例数据”。
